---
description: 制定计划时必须包含原始需求描述，后续迭代需追踪需求变更
alwaysApply: true
---

# 计划中必须包含需求描述

## 创建计划时

在调用 CreatePlan 生成计划时，plan 内容的**最前面**必须包含一个 `## 原始需求` 章节，将用户提出的完整需求原文（或精炼摘要）写入其中，确保计划具备完整的上下文自解释能力。

## 迭代计划时

当用户要求修改或迭代计划时，必须在计划中维护一个 `## 需求变更记录` 章节，按时间顺序记录每次需求的变化内容，格式如下：

```markdown
## 需求变更记录

- **迭代 1**：<变更描述，说明新增/修改/删除了哪些需求点>
- **迭代 2**：<变更描述>
```

这样做的目的是让任何人（包括未来的 AI 会话）仅凭计划文件就能完整理解需求的来龙去脉。
